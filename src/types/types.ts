export interface AssetItem {
  id: string;
  type: string;
  body: string;
  image: string;
  linkText: string;
}

export interface Testifiers {
  id: string;
  text: string;
  image: string;
  name: string;
}